{
  "name": "@test-lerna/root",
  "version": "1.0.0",
  "description": "Sample how to use lerna to make a monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish:nightly": "lerna publish --yes --no-verify-access --no-git-reset --canary --preid next.$(git rev-parse HEAD) --dist-tag next"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dapplion/lerna-monorepo-post.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dapplion/lerna-monorepo-post/issues"
  },
  "homepage": "https://github.com/dapplion/lerna-monorepo-post#readme",
  "devDependencies": {
    "lerna": "^3.21.0"
  }
}
